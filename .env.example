# Monad testnet (fill from Monad testnet docs); local fallback: http://127.0.0.1:8545
MONAD_RPC_URL=
MONAD_CHAIN_ID=
TOKEN_URI=https://pharmaguard.example/metadata/{id}.json

# Deployed contract (filled after Phase 2 deploy)
CONTRACT_ADDRESS=

# Demo account private keys (testnet only — never reuse real keys)
ADMIN_PK=
ORACLE_PK=
MANUFACTURER_PK=
DISTRIBUTOR_PK=
PHARMACY_A_PK=
PHARMACY_B_PK=

# Monad testnet; local fallback: http://127.0.0.1:8545 (chainId 31337 for anvil)
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_CHAIN_ID=10143
TOKEN_URI=https://pharmaguard.example/metadata/{id}.json

# Deployed contract (filled after Phase 2 deploy)
CONTRACT_ADDRESS=

# Demo account private keys (testnet only — never reuse real keys)
ADMIN_PK=
ORACLE_PK=
MANUFACTURER_PK=
DISTRIBUTOR_PK=
PHARMACY_A_PK=
PHARMACY_B_PK=
